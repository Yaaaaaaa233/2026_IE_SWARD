# AI 与人类协作者入口

适用于 Codex、ZCode、DSH 及其他在本仓库工作的工具和成员。本文是唯一 agent 规则入口；`AGENT.md` 仅作兼容指引。不同客户端未必自动加载本文件，新会话需显式要求读取。

## 开始工作

1. 读取 [文档导航](docs/README.md) → [当前状态](docs/DEVELOPMENT_STATUS.md) → [协作规范](docs/COLLABORATION.md)。
2. 运行 `git status --short`，确认分支、远端与已有改动。不得覆盖、清理、暂存或提交来源不明的改动。
3. 在 [任务登记](docs/tasks/README.md) 找到或创建任务，确认责任人、执行 agent、写入范围、依赖和验收要求。纯问答不要求建任务。
4. 涉及数据先读 [数据边界](docs/DATA_POLICY.md)；涉及实验或图表先读 [验收规范](docs/ACCEPTANCE.md)。
5. 没有认领、冲突未解决或依赖未具备时，不擅自修改其他工作线的共享接口；可继续任务内不受影响的工作。

## 必须遵守

- **路线与治理分开：** 算法路线尚未定稿。路线草案、临时口径、示意结果不得冒充正式决策或已验证成绩。当前状态只在状态页维护。
- **跨设备：** 仓库只记录相对路径和配置模板；机器路径写入被忽略的本地配置。原始数据通过组委会或团队受控渠道获取，不通过公开 Git 传递。
- **数据边界：** 这是公开仓库。原始数据、车辆／司机明细、折外预测、模型产物及数据派生图表默认不入库。脱敏或汇总不自动等于允许公开。
- **因果边界：** 特征和校准仅使用预测时点可见的数据；标签、切分、单位和时间契约的变更必须记录决策及受影响实验，不能静默改变比较条件。
- **结果诚实：** 未运行写未运行；环境失败与算法失败分开；不填虚构 AUC、通过率或审核人。治理检查通过不代表模型通过验收。
- **可审查：** 一次任务一个分支，避免多人共写同一工作目录；没有明确授权不强推、不重写他人历史、不自动合并 PR。
- **最小变更：** 只实现本次任务，不借治理初始化决定模型、依赖栈或成员分工。公共文件由协调者归并，避免所有 agent 同时改状态页。

## 结束工作

1. 运行与改动相关的检查。治理、目录或文档变更至少运行：
   ```sh
   python tools/check_governance.py
   python -m unittest discover -s tests -v
   ```
2. 根据 [可视化验收规范](docs/ACCEPTANCE.md) 提交阶段报告；原始结果保存于受控目录，公开仓库仅提交允许公开的说明。
3. 更新任务自身记录，新增一篇 [交接简报](docs/worklog/README.md)。状态或结论边界确有变化时再更新状态页，不机械改所有文档。
4. 提交前只暂存明确文件，运行 `python tools/check_governance.py --scope index`。检查失败必须非零退出；不能通过删掉失败项制造通过。
5. 汇报改了什么、检查了什么、尚未证明什么、下一个接手者从哪里继续。实现完成标为 `review`；未经真实复核不得标为 `accepted`。

规则版本与真实贡献见 [治理初始化记录](docs/decisions/ADR-0001-governance-bootstrap.md)。用户在当前任务中的明确授权优先，不能把本文解释成对所有日常操作都重复请示。
