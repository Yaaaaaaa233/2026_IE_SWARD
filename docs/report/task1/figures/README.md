# figures/ 图片目录

全组共用。放入图片后在正文用 `\includegraphics{文件名}` 引用（已设 graphicspath，无需写目录前缀）。

建议清单：
- `pipeline.png` —— 总体技术路线图（第1章，组长/建模线提供）
- 清洗流程图、数据版本关系图（第3章，数据线可选）
- 特征重要性图、AUC 曲线、折间 AUC 柱状图（第7章，建模线）

格式建议：矢量图用 PDF，位图用 PNG（导出 ≥300 dpi 或 2x 缩放，避免印刷模糊）。
