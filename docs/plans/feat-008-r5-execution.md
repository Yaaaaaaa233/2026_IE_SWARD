# FEAT-008 r5 执行方案：P1–P4 修复版候选（轨迹/同群真正入模）＋预登记一次判定

状态：accepted（叶安 2026-09-23 裁定 r5 修复版一次；预登记先于执行落盘）
上位方案：[task1-optimization-plan-v1](task1-optimization-plan-v1.md) §4 阶段 1；验收：[task1-optimization-acceptance-v1](task1-optimization-acceptance-v1.md) 迭代轮次行。
设计输入：[FEAT-007 r4](feat-007-r4-execution.md) 归因消融（基座为唯一显著块、**轨迹/事件结构 0 列入模**——新信息路线未被真正检验）＋ L2 问题清单 P1–P5（受控台账）。

## 1. 目标与边界

修复 L2 发现的 P1–P4（P5 为文档补写），把预登记主力族（轨迹疲劳结构/同群相对化）**真正放进候选**，做**一次**预登记判定。这是新信息路线的第一次真实检验：过门 → 基线棘轮；未过门 → 负结果登记并**收口进阶段 3**（不再有 r6，防迭代期无限延长）。时限一天（09-23 内出结果）。

## 2. 修复清单与预登记口径（执行中不得擅改）

| # | 问题 | 预登记修复口径 |
| --- | --- | --- |
| P1 | 轨迹坐标跳变污染里程 | 相邻点隐含速度 >70 m/s（≈252km/h，货车物理上限外）判**坐标跳变**：该步不计里程、并切断行程段；产出质量列 `f3_traj_jump_steps`（跳变步计数，供任务二） |
| P2 | gap>180min 切段在"停车仍 5 分钟一报"下退化 | 切段语义改**双切段**：≥30min 停车（连续 speed=0 或位移<50m 持续 30min）或数据断档 >180min 均切段；"连续驾驶段"=相邻切段间的行驶段（段内 <30min 短停计入行驶）；产出最长连续驾驶、>4h 占比、深夜（23–5）连续驾驶时长 |
| P3 | 同群群键退化（config 列名不符静默 NaN） | 修正为 `highway_ratio`；群键＝能源类型 × 高速里程占比**车队中位二分**（回归预登记口径）；同群统计仍折内拟合 |
| P4 | 深夜两套小时口径差 8h | 统一北京时间 +8h 口径（与 core 一致）；trajectory 侧 hour 函数对齐 |
| P5 | 画像折月系数未入验收字面 | 文档补写：画像偏差率＝(窗内实际日均×30.44 / 画像月均) − 1 |

**候选构成规则（修复 r4 的 40 列预算挤占缺陷，预登记）**：`select_new_columns` 改**族配额制**——按列名前缀分族（score/hist/cohort/profile/night/spell/speed/spatial/rhythm/chain/ems/event/其他），**每族上限 6 列、族内按声明列序保先、总量 ≤40**；`f3_night_*` 的**分场景变体不入模**（只留合并版/比率版）；综合分（f3_score_*）恒优先。族→前缀精确映射随执行登记（不改规则只对名字）。

**判定预登记（与 r4 同构，只判一次）**：候选＝F3R5_v1（lean 基座 ＋ 修复后新增族），对 RF@F0_v1 车辆级配对 bootstrap（2,000/95%/seed42 主判定；seed7/2026 旁证不改判）；判据 ADR-0004 不放松（CI 下界>0 且点估计>0；容忍度 Recall@100/Brier）；归因消融同 5 块口径（仅归因）。

## 3. 实现与运行

1. 代码修复（`accident_pipeline_f3/` 内，含合成测试）：P1 跳变过滤、P2 停车切段、P4 时区统一、族配额 select_new_columns；P3 为本地配置修正。
2. 重跑 `run-all`（一遍扫描约 80–90 分钟）→ interface（收尾通则与契约断言不变）。
3. 统一评估器 ×3 种子 → analyze 配对判定（一次）→ 归因消融。

## 4. 验收（迭代轮次轻量口径）

L1 全项＋版本三件套＋合成测试新增（跳变切断、停车切段边界 30min、时区统一、族配额确定性、分场景夜间不入模）；L2 轻量（P1 修复车 95202153 里程复核、P2 切段抽 2 车人工对照、P3 群键分布核查）；L3 图组 B/D/E 同 r4。过门才补全完整 L2/L3 并棘轮。

## 5. 时间盒与收口

09-23 午前代码修复与测试 → 午后重扫 → 傍晚判定与登记。**无论过门与否，r5 即最后一轮特征侧判定**；未过门按 §1 收口进阶段 3（EVAL-003 登记由人工宣告启动）。
