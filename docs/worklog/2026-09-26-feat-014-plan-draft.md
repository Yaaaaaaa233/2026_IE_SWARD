# 2026-09-26 FEAT-014 持续探索方案代拟

- 任务：[FEAT-014](../tasks/FEAT-014.json)，状态 `proposed`；负责人叶安，代拟 Codex，真实复核尚未发生。
- 分支：`work/feat-014-exploration-plan`；基准提交：`e8caaf49e77703aa57ed80f0787650cb3ed0e750`。
- 当前步骤：方案代拟；本次写入仅方案、任务与本交接三文件。使用已有隔离工作目录，原工作区和其他算法线不改动。

## 本次完成

核对 FEAT-009 v2、评价方案 v1.3、FEAT-012/013 实际实现和 MODEL-008 迭代经验，形成[持续迭代方案](../plans/feat-014-adaptive-feature-model-exploration.md)。固定叶安线原有评定标准，区分 RF 基线、F3 固定优化对照和滚动父版本；开放特征重新表达、针对性新特征、算法适配及误差反馈，列明第一批六个版位、后续选择规则、30 版／24 小时预算、每步机器和图表验收、记录与回滚。

用户先要求形成方案，随后明确要求本方案提交 Git，并指出应在文档中说明纪律例外。本次未开始训练；未把全项目 EVAL-001 正式采纳作为新的阻塞项。方案描述的 FEAT-014 CLI 尚未实现。未修改共享状态页或公共契约，未新增效果数字。按 roadmap_requirements §2.6，本应暂不提交待确认草案；本次依用户明确指示作单次例外，并在方案第 9 节标注其有限范围。该例外仅针对入库方案文本，不代表 FEAT-014 实验已获准开跑，亦不改变项目纪律。

## 检查

`python3 tools/check_governance.py` 通过（working-tree，354 个文件）；此前 `python3 -m unittest discover -s tests -v` 通过（89 项）。本次更新例外说明；实验效果、FEAT-014 机器验收器及真实图表均为 `not_run`，没有新模型证据；单测通过不表示新探索方案已经运行。

## 下一步

入库本身仅保存方案文本。之后如要执行，再登记明确的接受版本与执行状态；先做 E0 输入与旧对照锁定，按 E1↔E2 小批次滚动探索，预算内连续推进，E3 统一输出候选和完整经验。任务完成技术执行后进入 `review`，不预填人工接受记录。
