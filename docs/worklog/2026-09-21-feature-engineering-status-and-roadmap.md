# 2026-09-21 特征工程现状、接口与路线图整理

- 任务：[FEAT-001](../tasks/FEAT-001.json)
- 分支：`main`（直推，ADR-0003 模式）
- 基准提交：`0ebad4d`
- 人类责任人：待确认
- 执行者：Codex agent；输入为用户提供的 2026-09-20《今日工作报告》
- 复核状态：`review`，尚无真实复核人

## 做了什么

- 新增[特征工程当前工作、接口状态与路线图](../plans/feature-engineering-roadmap.md)，将原报告重组为一页结论、工作清单、端到端链路、A/B/C 三段接口矩阵、问题处置、阶段路线、交付边界与结论边界。
- 将“已实现”“实现者自测”“待团队复核”“待正式冻结”分开表达；探索性聚类不作为已验收模型成绩。
- 对齐现有数据契约、模型契约和总体执行路线图，明确两个优先接口问题：features 所有权重叠、接口层插补必须迁移到训练折内。
- 在 rebase 远端新增的 GOV-004 后，对齐已生效的数据交付规范：L2 受控交付采用可辨识包名、五件套、权威目录结构和接收联合对数。
- 公开版不复制原报告中的真实数据派生统计，也不提交任何 CSV、模型、车辆级明细或事故回放产物。
- 新增任务登记并更新 plans/tasks 索引。

## 验证

- `python tools/check_governance.py`：PASS（working-tree，67 个文件；已包含 rebase 后的 GOV-004）。
- `python -m unittest discover -s tests -v`：17 项全部通过。受限沙箱首次运行时，4 项需要在临时目录执行 `git init` 的快照测试因环境权限报错；在允许临时 `.git` 写入后原命令重跑通过，失败产生的临时目录已清理。
- `git diff --check`：通过。
- `python tools/check_governance.py --scope index`：PASS（index，67 个文件）。

| 维度 | 状态与范围 |
| --- | --- |
| 实现与契约 | 文档整理完成；公共数据与模型契约仍为草案，本文不改变其状态 |
| 实验效果 | `not_run`；本任务未运行数据或模型实验，也不新增可引用成绩 |
| 验收与证据 | 当前为实现者整理，待特征线负责人和接口消费者复核 |
| 环境与依赖 | 仅文档与治理检查；真实产物继续留在受控环境 |

## 决策与限制

- 无新增 ADR；本文是特征线细化 roadmap，不替代总体执行路线图和公共契约。
- 原工作报告中的数值可用于团队受控复核，但公开 Git 版本只保留不泄露真实派生统计的方法性信息。
- “本地已实现”不代表已登记为公共可运行模块；代码入口、manifest 和 F0 版本仍需后续任务补齐。

## 接手者下一步

1. 特征线负责人核对工作清单、接口载体和阈值描述，确认或修订后填写真实 reviewer。
2. R0 / 契约 v1.0 明确 features owner，并决定 B 面保留缺失值、模型线折内插补的具体格式。
3. 登记特征代码入口、字段字典、manifest 和 `feature_version`，完成 F-R1 复现验收。
4. 接入评估 harness 后执行事故回放、分组消融和监督基线；数字证据仍保存在受控报告中。
