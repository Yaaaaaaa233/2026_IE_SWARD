# 2026-09-23 方案B V5 入库（王健祺线当前登记最优）

- 任务／分支／基准提交：[MODEL-005](../tasks/MODEL-005.json)｜`work/SCORE-001-task2-roadmap`｜`7b473d9`
- 人类责任人：王健祺（组长；授权"V5 及其结果上传 git 以便任务二对接"）
- 执行者与 AI 协助：ZCode agent
- 复核状态：`review`——按 ADR-0005 双人竞优，对手线（叶安）或纪律管理可复算裁决

## 做了什么

- 入库 [models/solution_b_v5/](../../models/solution_b_v5/README.md)：V5 可复跑管线
  （common_v5.py 路径模板制 + run_v5.py 一键 F1 装载→五成员嵌套折外→分群校准→三类输出）。
- 输出合同：`oof_predictions_v5.csv`（裁决复算）、`model_score.csv`（任务二 Q1 对接：prob/rank_pct/calib_flag）、
  `forecast_result.csv`（官方三列）、`manifest.json`（输入 sha256 指纹，ADR-0005 要求的消费登记）。
- run_v5.py 内置 ADR-0004 判据自动报告：检测到受控 outputs 下的冻结基线 OOF 时输出配对区间与过门判定。
- V5 = F1 全特征 × 五成员（EBM×3 种子 + RF + EBM 无交互×2 种子，V3 折内贪心选出）× logit 融合
  × 分群校准（n0 折内按 Brier 选，V4 修复）。迭代史 V1–V4 方法层结论见 README。

## 验证

- 从仓库目录完整复跑 run_v5.py：输出与受控台账登记的 V5 折外预测一致（同种子确定性；数字不入公开仓库）；
- `python tools/check_governance.py --scope index` 与单测通过后提交。

## 决策与限制

- 判据状态（ADR-0004）：V5 对冻结基线（RF）配对区间下界 > 0——**过门**，王健祺线当前登记最优；
  正式数字与逐车预测在受控 `outputs/`，不入公开仓库。
- 开发期内部回测边界不变；任务二消费即冻结该 schema（只增列不改列，ADR-0005）。

## 接手者下一步

1. 任务二组按 SCORE-001 路线图消费 model_score（登记 manifest 指纹）；
2. 王健祺线继续迭代（条件强度特征+折内降维组合已在受控目录完成初验，显著正向；
   完整五成员版运行中，过门则按 ADR-0004 棘轮为新基线并升级 V5→V6 登记）；
3. 叶安线竞优对照共用同一裁决器。
