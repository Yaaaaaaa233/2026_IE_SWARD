# 2026-09-23 官方答疑公共口径对齐与范围收窄

- 任务／分支／基准提交：GOV-008；`main`；`3da9a6e`。
- 人类责任人：叶安。执行者：Codex。复核状态：`review`，未记录对本次文件的人工复核。

## 做了什么

叶安明确指出此前重跑方案过细，王健祺线仍在进行中，要求只先统一整个项目与官方答疑有关的基础事实。因此以 [ADR-0006](../decisions/ADR-0006-official-qa-foundation.md) 记录官方已明确的标签、时间、时段、画像、评测与提交口径，并把[调整方案](../plans/2026-09-23-official-qa-adjustment-plan.md)改写为公共口径清单。同步数据／模型契约、评价基座、顶层路线图、任务二交付描述和状态页；报告 LaTeX 入口与 README 明确旧章节数字失效。旧协议效果仍冻结。

## 验证

检查公共文件对官方规则的描述与历史快照状态；`python3 tools/check_governance.py` 通过，`python3 -m unittest discover -s tests -v` 的 28 项通过，JSON 解析与 `git diff --check` 通过。没有生成新版真实标签、折分、图表或模型数字。已有 FEAT-008、MODEL-005、EVAL-002 的外部工作树改动以及 FEAT-009 草稿未纳入本次提交。

## 决策与限制

ADR-0006 只接受已获答疑明确的公共事实；未遂“次”的计数单位、未来 40 天精确右端和时区仍开放。评价指标实现方法可复用，但旧标签、旧分层折、旧基线身份和旧 OOF 效果不得直接用于新口径判优。历史实验与报告章节保持原样并加显著警示，避免静默改写证据。已知若干算法实现仍用研究用深夜分桶或旧输出列头；本任务没有改动两条算法线的源码，运行产物不能因文档更新而自动视作符合新协议。

## 接手者下一步

数据／评估负责人闭合开放语义并发布有版本的新版标签、折分与验收证据；各算法负责人自行决定其特征、模型和实验的调整。报告各章负责人在新证据形成后分别修订旧数字。任务二负责人按“自定义结果 CSV＋模型／分析文档”准备交付。所有真实数据及派生结果继续留受控目录。
