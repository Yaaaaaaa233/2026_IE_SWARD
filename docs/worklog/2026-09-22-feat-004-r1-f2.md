# FEAT-004 r1（F2 主管线）执行记录（主体完成，待 L2/L3 人工验收）

- 任务：[FEAT-004](../tasks/FEAT-004.json)｜状态：review（L1 机器项完成；L2 人工抽样与 L3 图组待复核人）
- 执行方案：[feat-004-r1-execution](../plans/feat-004-r1-execution.md)；分支 `main`；基准 `58da230`
- 人类责任人：叶安（仓库建立与纪律管理）；执行：ZCode agent

## 做了什么

1. **F2 管线实现入库**：`accident_pipeline_f2/`（f2_core 纯函数＋pipeline 三遍扫描编排＋interface 组装＋cli）；配置模板入库、本地配置不入库；合成测试 5 项（对齐恢复已知旋转、校准分层/统一双走向、分位数精度、冷却合并、特征窗边界排除标签窗）——feature_engineering 测试 7/7 通过。
2. **三遍扫描全量运行**：A 对齐（充分统计量流式累计）→ B 分位数校准 → C 事件提取＋interface。产物落仓库外 feature_f2_v1_run/（冻结目录只读）。
3. **发现并修复一个跨模块工程缺陷**：pandas 3.0 下 pyarrow 将 `data_time` 推断为 `datetime64[s]`，`astype(int64)` 已是秒数、再整除 10⁹ 使所有时刻坍缩为 1（症状：伪 996 条事件、每车每场景恰 1 条、时长全 0、event_time 全为 1970-01-01）。修复为显式 `astype("datetime64[ns]")` 后重跑提取。同款缺陷影响探针 C 的死区窗分桶（尖峰/卡死/对齐结论不受影响），探针脚本已修复并重跑（结果见受控台账 §8）。
4. **统一评估器配对判定＋消融＋多种子**（结论与数字见受控台账 §8，公开侧只记结论）：F2 全量未过门；**方向异常子集 AUC +0.062——增益机理定位为"对齐救回"**；校准证实能源类型间阈值差异真实存在（分层生效）。

## 验证

- 合成测试 7/7；治理检查与单测随本批提交执行；模型侧评估器为冻结实现原样调用。
- 六场景事件量级与赛方参考资料预期同阶（急刹多于急加速，符合货车特性）；侧翻 0 条与"极低频"预期一致。

## 限制

- IMU 平均采样间隔约 20s/样本，"事件"语义为极端样本簇（组间隔 2s、冷却 30s），持续时长多数为 0——任务二消费事件表时按"极端行为样本"理解。
- 天然气类型仅 0.27M 行，其分层阈值的分位数估计噪声较大（登记为已知限制）。
- L2（对齐目检抽样＋事件抽读＋碰撞预警交叉）与 L3（图组 B/C/D）待复核人执行。

## 下一步（r2 候选方向，待纪律管理拍板）

1. 方向无关版六场景（模长触发）覆盖对齐失败的 125 辆；
2. 高频阈值放松一档（p99）提高计数方差；
3. 方案 B 分群/方向异常子集加权——放大已证实的"对齐救回"效应。
