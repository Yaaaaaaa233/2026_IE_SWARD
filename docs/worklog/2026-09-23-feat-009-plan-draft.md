# 2026-09-23 FEAT-009 建模优化方案代拟

- 任务／分支／基准提交：[FEAT-009](../tasks/FEAT-009.json)｜`main`（ADR-0003，仅方案入库）｜`3971d7e`
- 人类责任人：叶安
- 执行者与 AI 协助：Codex 代拟方案；真实实验未开始
- 方案确认：叶安 2026-09-23 在本任务会话确认“没问题，入库”，方案置 `ready`；任务实现／实验的复核人仍待登记

## 做了什么

依据既有 r5 收口、V5 复算与固定 V5 汇合诊断的结论边界，新增[已有特征建模优化方案](../plans/feat-009-feature-modeling-proposal.md)：S0 输入与泄漏审计、S1 折内学习器选择与一次主判定、S2 互补性诊断、S3 人工复核。方案为每步写明具体操作、机器验收、可视化验收与放行条件；不启动 r6 或修改王健祺线代码。

## 验证

`python3 tools/check_governance.py`：working-tree PASS；`python3 -m unittest discover -s tests -v`：28 项通过；`python3 -m json.tool docs/tasks/FEAT-009.json` 与 `git diff --check`：通过。当前宿主环境没有裸 `python` 命令，因此使用 `python3` 完成文档验证；方案中的 `python` 表示未来在项目虚拟环境下的目标入口。真实模型命令尚未实现、真实数据实验 `not_run`。

## 决策与限制

沿用 ADR-0004／0005 与任务一优化验收规程；本方案未改变基线或采纳门槛。此前诊断已使用同一批 OOF 标签，新实验仍属开发期内部证据。方案已由叶安人工确认可入库；此确认只覆盖方案，不代表实现、模型效果或 L2/L3 已验收。

## 接手者下一步

由执行 agent 在新会话先读已入库的本方案与项目纪律，报告当前步骤、输入版本和写入范围；按 S0 先审计，再由叶安按步骤放行。真实数据主判定只能在 S0 通过、S1 代码与检查齐备后执行。
