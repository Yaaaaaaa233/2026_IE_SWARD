# FEAT-006 r3（y 盲剪枝 bundle）执行记录

- 任务：[FEAT-006](../tasks/FEAT-006.json)｜状态：in-progress
- 执行方案：[feat-006-r3-execution](../plans/feat-006-r3-execution.md)；分支 `main`；基准 `590073a`
- 人类责任人：叶安；执行：ZCode agent

## 计划

1. 预登记选特征规则（已写入方案 §1，先于评估落盘）
2. 构建 pruned_bundle / lean 两候选＋保留/剔除清单
3. 统一评估器×6 运行（两候选×三种子）＋配对判定
4. 台账 §8＋worklog＋提交

## 结果

（真实数字见受控台账 §8，本节只记结论。）

- **构建**：按预登记 y 盲规则产出两候选——pruned_a（42 增量列/总 112，含确定性相关剪枝）与 lean_b（39 增量列/总 109，各族核心）。选特征全程未接触标签，保留/剔除清单落 selection_report.json。
- **配对判定（主种子 42，判据不放松）**：两候选均**未过门**（CI 跨零），容忍度全过（Brier 双双改善，Recall 退化在容限内）。但为四轮证据最强的一版：pruned_a CI 下界 −0.0027（四轮离门最近）、点估计 +0.0124 高于 full_r2 的 +0.0094——剪枝抗稀释假设方向成立；lean_b 三种子全部 ≥+0.011 且 seed7 CI 下界转正（旁证种子过门，主种子未过，按纪律不采纳——与 FEAT-003 多窗口族的镜像情形，口径一致）。
- **判读**：四轮总形态定型——真实效应 +0.01 量级、n=500 功效极限 ~0.015；单靠特征侧堆叠/剪枝已无法过门，与外部审计的天花板诊断一致。下一步选项（人工裁定）：子群放大对齐救回效应（+0.062，与王健祺方案 B 汇合）/ 转任务二 / 阶段 3 推进门。

## 结论与移交

- 本轮按预登记判据**未采纳**，负结果登记，基线不动（仍 RF@F0_v1）。版本三件套＝本 worklog＋FEAT-006.json＋台账 §8。
- 晚间汇报要点：预登记→执行→结果完整链条可展示；"剪枝方向成立但功效瓶颈"是本轮结论；lean_b 可作为后续子群/集成实验的特征底座候选。

