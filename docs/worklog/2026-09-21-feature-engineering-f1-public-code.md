# 2026-09-21 特征工程 F0/F1_v1 公开源码

- 任务／分支／基准提交：FEAT-002／main／bf9425f
- 人类责任人：待确认
- 执行者与 AI 协助：Codex agent
- 复核状态与真实复核人：实现者自测完成，待团队复核；复核人未指定

## 做了什么

- 新增 `feature_engineering/`，公开F0与F1_v1的源码、依赖、配置模板和合成测试。
- F0包含IMU流式聚合、统一模型接口和探索性聚类。
- F1包含历史险情、暴露归一、多尺度窗口、新近度、事件语义细分和重复字段排除。
- 新增公开方法说明，记录防泄漏边界和暂缓IMU阈值校准的规则。
- 更新任务索引、模块登记和本地配置忽略规则。
- 未纳入数据、真实统计、车辆级输出、模型、内部PDF或本地绝对路径。

## 验证

- 模块合成测试：待在目标仓库安装可选测试依赖后运行。
- 仓库治理检查：提交前运行工作树与暂存区两种范围。
- 仓库原有测试：提交前运行 `python -m unittest discover -s tests -v`。
- 真实数据结果不属于公开证据，本简报不记录数值。

## 决策与限制

- 遵守 [ADR-0003](../decisions/ADR-0003-no-pr-collaboration-mode.md) 的直推模式，但推送前仍需治理与暂存区检查。
- F1代码可运行不证明特征增益；真实消融尚需模型线按冻结折分复测。
- IMU阈值不足最小金标准样本时只输出暂缓状态，不能使用标签窗口调参。
- 根CI只验证治理和现有轻量测试；特征模块依赖需单独安装后运行模块测试。

## 接手者下一步

1. 安装 `feature_engineering[test]` 并运行模块合成测试。
2. 由特征线真实负责人复核字段定义、事件映射、阈值门槛与配置路径。
3. 按固定折分做F0与F1分组消融；真实结果保存受控目录。
4. 复核通过后更新FEAT-002的 reviewer、reviewed_at 和状态；未复核前保持 `review`。
