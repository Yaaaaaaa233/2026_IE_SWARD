# FEAT-003 阶段0：F1 真实运行＋双探针（进行中）

- 任务：[FEAT-003](../tasks/FEAT-003.json)｜状态：active（步骤 A/B 完成，C/D 待执行）
- 执行方案：[feat-003-execution](../plans/feat-003-execution.md)
- 分支：`main`（直推）；基准提交：`81e1c3b`（登记提交 `83b0944`）
- 人类责任人：叶安（仓库建立与纪律管理）；执行：ZCode agent（ds-executor 派发两次被 Mimosa 内容风险规则拦截，步骤 A/B 由主会话直接执行，全程过 Write/Bash 扫描通道）

## 进度与结果

1. **步骤 A：F1 真实运行——完成**。F1 管线首次在真实契约数据运行（约 5 秒）；产物落独立新目录（特征工程冻结目录只读未触碰）。硬检查全过：行数/正类/各折与 F0 一致，**y 与 fold 逐车完全一致**；新增 58 列（历史险情/碰撞预警/多尺度窗口/暴露归一四族+IMU 归一）；3 个未覆盖列系 F0 已有 20 天计数的重命名副本（冗余，合理排除）。IMU 阈值校准按设计暂缓（金标准不足，预期内）；F1 事件扫描行数与清洗守恒数一致。指纹与明细：`outputs/feat-003/round-1/step_a_checks.md`（受控）＋台账 §8。
2. **步骤 B：配对评估＋消融——完成**。统一评估器（冻结协议）跑 F1 全量与四族消融共 5 次 OOF，对冻结基线做车辆级配对 bootstrap（PREREG），另做多种子旁证（42/7/2026）与 full_f1 的 Top-K/Brier 读数。**判定（详细数字见受控台账 §8）**：
   - F1 全量：**未过门**（区间跨零）；
   - 多尺度窗口族：种子 42 下**边缘过门**（下界贴近 0、非实际意义增益），多种子方向同向但区间不稳——叠加"四族选最优"的选择效应，**建议暂不采纳、不棘轮**，并入 F2 轮次复验（待纪律管理裁定）；
   - 历史险情族：RF 无增益（LR 方向为正但不显著）——正类历史事件稀疏、树难分裂，F2 中改与暴露/时段交叉构造；
   - 碰撞预警族微负、暴露归一族方向为正不显著；
   - 多种子四组 ΔAUC 方向全部为正；full_f1 的 Recall@100 与 Brier 同向改善、容差达标。
3. **步骤 C/D：双探针——待执行**（陀螺质量审计、对齐可行性；F2 设计输入）。

## 验证

- 评估器为冻结实现原样调用（--model-input 传入，未改代码）；`feature_version_f1` 常数列进入特征矩阵属冻结排除表未含该名的既定行为（对 RF 无分裂增益、对 LR 近零权重，惰性，已登记）。
- 治理检查与单元测试随记录提交执行；真实数值仅落 `outputs/feat-003/round-1/` 与受控台账（DATA_POLICY）。

## 限制

- 步骤 B 为单折分表、预登记种子口径；多种子仅作旁证不作判据（ADR-0004）。
- 本轮结论不外推到 F2（六场景特征集不同，将整体复验）。

## 下一步

1. 步骤 C/D 双探针（含 IMU 行数口径核查）→ 产出 FEAT-004-r1（F2 主管线）设计输入；
2. 纪律管理对"multiwindow 暂不采纳"建议裁定；若认可，F2 特征集并入多窗口族一并复验；
3. FEAT-002 review 收口（引用本任务证据）。
