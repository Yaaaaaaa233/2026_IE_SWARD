# FEAT-003 阶段0：F1 真实运行＋双探针（主体完成，待 L2/L3 人工验收）

- 任务：[FEAT-003](../tasks/FEAT-003.json)｜状态：review（机器验收全过；L2 人工抽样与 L3 图组待复核人）
- 执行方案：[feat-003-execution](../plans/feat-003-execution.md)
- 分支：`main`（直推）；基准提交：`81e1c3b`（登记 `83b0944`、A/B 记录 `6b707d1`）
- 人类责任人：叶安（仓库建立与纪律管理）；执行：ZCode agent（ds-executor 两次派发被 Mimosa 内容风险规则拦截，全部由主会话直接执行，过 Write/Bash 扫描通道）

## 结果总览（详细数字见受控台账 §8 与 outputs/）

1. **步骤 A（F1 真实运行）完成**：硬检查全过（行数/正类/各折一致、y 与 fold 逐车同 F0）；新增 58 列四族；指纹登记；IMU 校准按设计暂缓；事件扫描行数与清洗守恒一致。
2. **步骤 B（配对评估＋四族消融＋多种子）完成**：F1 全量未过门（区间跨零）；多尺度窗口族种子 42 边缘过门、多种子区间不稳，**纪律管理裁定暂不采纳、不棘轮，多窗口族并入 F2 复验**；多种子方向全部同向为正；历史险情族改交叉构造进 F2。
3. **步骤 C（陀螺质量审计）完成**：抽样 40 辆窗内无尖峰（max 远低于物理界）、无卡死游程——**F2 stage 0 质量层简化为轻量断言**（原方案减负）。（更正 2026-09-22 v3：原"死区窗 0"子项结论撤销——epoch 转换缺陷修复后重跑显示 60s 窗方差判据在本数据约 10-20s 采样密度下把平稳巡航也判为"死区"，判据失效；二阶段 C3 判据的适用性同步存疑待数据线复核。尖峰/卡死/对齐/行数结论经重跑复核维持。）
4. **步骤 D（对齐可行性）完成**：重力＋水平 PCA 方案可行；可靠组与**方向异常组通过率相同**——约 65% 方向异常车可救回前向/横向特征；失败车（方差比不足）保留方向无关特征＋对齐失败标志。
5. **IMU 行数口径收口（附带成果）**：本机全量解析 30 分片与特征线 manifest **完全一致（差 0）**；L1 校验报告差 3.7% 属报告侧缺陷——数据本体与特征消费无恙，登记为报告侧债务。
6. **FEAT-002 收口**：凭本任务证据置 accepted（复核人叶安，接手人条款）。

## 验证

- 统一评估器为冻结实现原样调用；探针脚本可重跑（固定抽样种子 42）；全部真实数字仅落受控目录。
- 治理检查与单元测试随本记录提交执行。

## 限制

- 探针为 40 辆分层抽样（结论外推到全量时按通过率估计不确定性）；对齐判据阈值为工程初值，F2 定稿前可调。
- 死区窗判据以 IMU 自带速度替代轨迹速度（自包含实现），与二阶段 C3 的轨迹速度口径存在差异，已登记。

## 下一步

1. L2 人工验收：抽 10 辆从 events_clean 人工数历史险情对 `f1_prior_*`；对齐抽样 3–5 辆目检窗段（复核人：叶安或组长王健祺）。
2. L3 图组 A/B/D 生成（受控 outputs/figures/feat-003/）。
3. **FEAT-004-r1（F2 主管线）方案成文并登记**：设计输入已齐——质量层简化、对齐矩阵可行（65% 异常车可救）、六场景阈值分能源类型初值、多窗口族并入复验、prior 族改交叉构造。
