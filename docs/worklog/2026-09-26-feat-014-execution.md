# 2026-09-26 FEAT-014 执行简报

- 任务：[FEAT-014](../tasks/FEAT-014.json)，状态 `active`；负责人叶安；执行 Codex。叶安明确授权按已提交方案连续执行，不在候选版本之间等待逐次确认。
- 方案：[FEAT-014 持续探索方案](../plans/feat-014-adaptive-feature-model-exploration.md)。
- 代码分支：`work/feat-014-execution`；初始执行提交 `321d218eebd947be7b697a637fe2ea6176590f00`。
- 受控运行：`outputs/feat-014/20260926-e0-r2/`（被 Git 忽略）；真实 OOF、预测、指标、数据指纹和图均只存于此处。
- 当前：E0、B001 的 E1、首轮 E2 机器审计通过；B002 已按 B001 反馈登记，继续执行中。A0/A1 与真实实验图的人类复核仍待叶安记录。

## 已完成

E0 锁定 FEAT-009 v2 F3 输入、冻结标签／折分、历史 RF 与 F3 EBM-A 对照、FEAT-011 列台账和车辆日表来源。历史 FEAT-012/013 的 36 个 OOF 版本重新核验通过；FEAT-012 full EBM-A 与固定 F3 OOF 对齐。G1 五个候选日面板率／覆盖字段只使用代理特征窗，保留来源、分母、缺失语义与截止审计。正式 E0 的独立审计通过，A0 时间窗图及 A1 特征族图已生成。

B001 在运行前登记六个版本，覆盖正则逻辑回归、精简 RF／HGB、全量及精简 EBM 交互、历史视角加 G1。六个五折 OOF 均完成，独立 E1 复算通过，验证了样本绑定、概率、主辅指标、配对区间、Top100 父比较和折内拟合范围。结果只供本开发集内迭代；置信区间不能消除反复查看同一代理标签造成的选型偏差。该批没有形成“已验证提升”的结论。

新增 `diagnose.py` 与 `audit_e2.py`：从保存的 OOF 重算相对固定对照的 AUC、AP、Recall@100、Brier、配对区间及 Top100 纠错／新增错误，并比较两个候选的正类覆盖互补性。B001 的诊断独立复算通过，产生 C0/C1 图；B1/B2 图也由诊断器按真实批次名重新绘制，修正了运行器初版的图标题标识。诊断只给下一批提供候选假设，不输出车辆身份或逐车错误名单。

依据第一批观察，B002 预登记五个有明确对照的版本：移除 G1 的匹配消融；精简 EBM 交互数 0／2／5 对照；历史视角加 G1 的交互对照；以及相同视角的平滑容量对照。计划已写入受控运行目录并绑定 E0 锁，未查看 B002 结果后再改参数。

## 验证与限制

- E0、B001 E1、B001 E2 独立审计均通过；FEAT-014 针对性合成合同测试通过。
- 输入时间窗、正类、五折与固定对照沿用该任务锁定的协议，没有更换评价基座或窗口。
- 当前版本都属于代理开发期探索，不代表官方未来期成绩或跨算法线的统一评定结论。
- 合成版式预览的实际验收以先前用户回复为准；真实 A0/A1、B1/B2、C0/C1 图的人工复核状态仍为 `pending`，机器出图不冒充叶安验收。
- B001 个别运行有 scikit-learn 弃用提示和物理核心检测提示，均未造成拟合失败；执行代码与环境版本已锁定，后续若改运行器须形成独立代码版本。

## 下一步

完成 B002 五个已登记版本，复算独立审计并按锁定规则解释；随后按 E2 结果登记少量固定权重融合或其他有归因候选。预算内连续迭代，最后执行种子稳定性、独立复算、全版本台账及交接；真实图表、结论与使用边界最终由叶安复核，不将状态提前标为 `accepted`。
