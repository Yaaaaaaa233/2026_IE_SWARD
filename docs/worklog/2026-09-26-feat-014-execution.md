# 2026-09-26 FEAT-014 执行简报

- 任务：[FEAT-014](../tasks/FEAT-014.json)，状态 `active`；负责人叶安；执行 Codex；方案经叶安明确指示启动。
- 方案：[FEAT-014 持续探索方案](../plans/feat-014-adaptive-feature-model-exploration.md)。
- 分支：`work/feat-014-execution`；起点 `7fa85505adc7dc5cebe6653a5c9bcd20acc1a475`。
- 当前步骤：E0 输入、固定对照、历史 OOF 与 G1 日表来源审计。写入范围为 FEAT-014 代码、针对性测试、任务／索引／状态／交接文件；真实派生值留受控输出。

## 已完成

实现受控 E0 `prepare` 与独立 `audit`，锁定 FEAT-009 v2 F3 输入、原始 RF OOF、F3 EBM-A OOF、FEAT-011 列台账及车辆日表。对 FEAT-012/013 的既有 OOF 运行独立指标复算，复核逐车输入、标签、折号、概率范围、既有融合和已登记哈希。两轮历史产物审计通过；FEAT-012 full EBM-A 与 FEAT-009 F3 OOF 一致。

对 61 天车辆日表只在代理特征窗内生成五个 G1 候选：单位里程事件段率、单位驾驶小时事件段率、事件日占观测日比例、逐日事件率变异、活跃日比例。检查 GPS 车辆键、车辆／日期唯一、事件计数和暴露字段完整、输入行序守恒。截止日及之后的记录不进入候选特征。G1 来源及定义、指纹和真实特征均保留于忽略的 `outputs/feat-014/`。

E0 初次输出 `outputs/feat-014/20260926-e0-r1/` 暴露审计读取 CSV 时车辆键推断为整数的问题；保留失败审计记录，修复为字符串读取后 E0 审计通过。该初次产物在执行代码尚未提交时生成，且其时间窗图需要重新排版，因此仅作调试证据；候选 OOF 未运行。正式 E0 锁定将在执行代码提交后以新 run ID 重做，不覆盖 r1。

已建第一批 B001 预登记和模型运行／审计入口。目标批次含逻辑回归、精简 RF、精简 HGB、全量与精简 EBM 交互以及历史视角加 G1 五类测试；所有数据划分保持冻结。当前尚未开始模型拟合。

## 验证与限制

针对性合成合同测试通过，覆盖时间窗右端排除、日表分母为零、重复车辆日键、未知车辆及 OOF 行重排；治理检查通过。E0 的 A0/A1 图表已生成并经实现者检查版式；人工视觉复核仍待叶安记录。实施者单元测试不替代真实 OOF 独立审计。

E0 第一次复核运行的检查器对两轮历史 OOF 均重算通过。FEAT-014 最终 E0 run、代码快照、B001 完整图表及负向审计仍待重做／完成；E1 实际结果尚未运行，不能引用新成绩。

## 下一步

完成实现与治理索引后提交执行代码；用提交后的干净代码新建正式 E0 run，复核锁定哈希及 A0/A1 图表。保存 B/C/D 合成样张后运行 B001；完成独立复算和错误诊断后，以本方案预算逐批选择后续方向。任何真实 OOF、AUC、名单、图或数据指纹都不得写入公开 Git。
