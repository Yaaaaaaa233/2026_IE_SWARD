# 2026-09-23 官方答疑后的冻结与方案交接

- 任务／分支／基准提交：GOV-008；`main`；`ba929d9`。
- 人类责任人：叶安。执行者：Codex。复核状态：方案 `review`，未记录人类接受。

## 做了什么

依据叶安提供的 0923 答疑文字版，把旧任务一标签／折分效果实验和 FEAT-009 S1 置于冻结状态；旧 S0 的历史审计结论保留，但不再放行新主判定。起草[调整方案](../plans/2026-09-23-official-qa-adjustment-plan.md)，按协议、数据、特征、模型、任务二和提交六个接口列出迁移与验收门。当前工作树中 FEAT-008、MODEL-005、EVAL-002 的具名复核记录由叶安确认，仍属其他改动，未纳入本任务；FEAT-009 的 S1 建模草稿与依赖改动也未当成已完成实现提交。

## 验证

只读复核答疑三页、仓库原赛题／数据说明和当前实现；本机受控事件数据的汇总审计支持“旧正类中存在仅一次未遂车辆”，并显示同日多条未遂对计数单位有影响。真实汇总与源路径不进公开 Git。`python3 tools/check_governance.py` 通过；`python3 -m unittest discover -s tests -v` 的 28 项通过；本机 S1 前置入口以空配置被 `blocked` 状态拒绝，未读取模型输入。没有运行新版真实模型。

## 决策与限制

冻结由叶安在本任务中明确要求；调整方案仍是待复核稿。按照 [ADR-0004](../decisions/ADR-0004-baseline-freeze.md) 的升版规则，标签／折分和公共评估口径需新 ADR 接受后才能重跑并声明效果。答疑未明确同日未遂计数单位，也未精确定义评测窗口右端；画像统计期跨过旧代理回测截点，必须先做特征可见性审计。旧任务的实现复核不等于新协议下的效果采纳。

## 接手者下一步

先完成调整方案 R0/R1：取得赛事方对两处计数／时间歧义的答复，登记新 ADR、版本和各线 write_scope；再重建输入并按 R2–R4 验收。解除 FEAT-009 阻塞前须有新版 S0 与新版基线／V5 证据。任何真实数字、车辆明细、输入指纹和图表继续留在受控目录。
