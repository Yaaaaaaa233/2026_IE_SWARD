# 2026-09-23 FEAT-009 S0 输入锁定与无泄漏审计

- 任务：[FEAT-009](../tasks/FEAT-009.json)｜分支：`main`｜基准提交：`34025373e2c522de204a93fd51eedb6bbb164a49`
- 人类责任人：叶安｜执行者：Codex
- 复核状态：叶安于 2026-09-23 确认 S0 审计结论与严格嵌套 S1 方向；S1 尚未放行

## 做了什么

- 新增 `feature_engineering/experiments/task1_feature_modeling/`，包括 S0 审计器、公开配置格式示例和合成正负例。审计器检查 F1、F3R5、V5 OOF、冻结 RF OOF 的逐车主键／标签／折号，核对时间字段、概率范围、输入指纹和 F3 字段清单，并输出受控报告与三张验收图。
- 使用受控本机配置完成 S0。逐车对齐和 V5 输入 manifest 指纹核验通过；F3 原 manifest 的 `PENDING` 占位值由本次审计对实际输入文件重新计算并保存，未冒充上游指纹登记。
- 代码复核确认 F3 筛列使用全表特征分布；F3 同群特征按外层五折预计算，直接用于内层三折选型会让部分内层训练特征使用内层验证分布；V5 聚类分组由全量无标签 F1 特征拟合。它们不等同于目标标签直接泄漏，但会限制严格归纳解释。
- 前置任务 FEAT-008、MODEL-005、EVAL-002 仍登记为 `review`，任务记录均没有具名 reviewer。V5 manifest 记录输入指纹，但没有登记 OOF 输出校验和；本次 S0 后另用清单对应的原始 F1、聚类、折分和目标车辆输入，在隔离临时目录重跑 `models/solution_b_v5/run_v5.py`。重跑与 source manifest 的版本及全部输入哈希一致；500 行 `sample_id`、车辆、标签、折号、分组一致，OOF 概率差异仅为浮点舍入级。受控复跑文件和摘要保存在 `outputs/feat-009/s0/v5_reproduction/`；原 manifest 缺输出校验和仍作为证据局限保留。
- 更新 FEAT-009 执行方案，明确 S1 必须在内层训练数据上重做筛列和同群变换；V5 比较须注明全量聚类的转导式边界。

## 验证

- `python3 -m unittest feature_engineering.experiments.task1_feature_modeling.test_audit -v`：合成对齐输入通过；构造标签不一致输入时 CLI 非零退出。
- `python3 feature_engineering/experiments/task1_feature_modeling/audit.py --config configs/feat-009.local.toml`：机器检查通过；人工放行仍待负责人复核。
- 受控产物：`outputs/feat-009/s0/acceptance.md`、`audit.json`、`manifest.json`、`issues.json`、`figures/`。这些文件含真实数据派生内容与指纹，被 `.gitignore` 排除，不入公开仓库。
- `python3 tools/check_governance.py` 与 `python3 tools/check_governance.py --scope index`：通过。
- `python3 -m unittest discover -s tests -v`：28 项通过；本任务合成测试 2 项通过；`git diff --cached --check`：通过。

## 决策与限制

- S0 未训练候选模型，未计算 FEAT-009 效果指标；没有产生任何模型过门结论。
- 叶安已确认 S0 结论及严格嵌套方向。S1 仍须在阶段前置样张与建议标准人工确认并入 Git、FEAT-008／MODEL-005／EVAL-002 获得可追溯复核依据、候选依赖具备之后才可启动。若不能按方案重建，先修订执行基线，再决定是否做转导式诊断。
- 实验方案仍遵守 [ADR-0004](../decisions/ADR-0004-baseline-freeze.md) 和 [ADR-0005](../decisions/ADR-0005-task1-dual-track-and-task2-split.md)。

## 接手者下一步

叶安已检查并确认 S0 审计边界与严格嵌套方向。接手者先完成 S1 阶段前置图样复核与候选依赖核验，再按更新后的方案实现内层折重建；前置任务 review 和候选环境齐备后才做候选池预跑与一次主判定。S0 报告和 V5 复跑证据是本机受控材料，不通过 Git 同步。
