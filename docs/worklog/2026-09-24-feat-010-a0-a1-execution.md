# FEAT-010 A0/A1 执行与复核交接（2026-09-24）

- 任务／分支／基准提交：[FEAT-010](../tasks/FEAT-010.json)；`main`，按 ADR-0003 无 PR 模式直接协作；`e2c55e3`。
- 人类责任人：叶安。
- 执行者与 AI 协助：Codex，按叶安连续执行 A0/A1。
- 复核状态与真实复核人：叶安已复核 A0 标签图、A1 时间窗图和 B0 消融流程的**合成样张**，三张均通过；真实数据图、来源与标签证据尚待复核。
- 当前状态：A0/A1 技术运行与机器检查完成，真实数据来源和图表人工复核待记录；B0/B1 仍受 EVAL-001、EVAL-003 及 FEAT-009 实际复核门约束。

## 已完成与正在执行

1. 生成 A0/A1/B0 三张合成可视化验收样张并交叶安实际复核；复核结果已记入 FEAT-010 任务记录。该通过仅适用于样张，不等同于真实数据或模型效果验收。
2. 执行 A0 机器审计并将逐车核验、版本绑定、来源检查和真实数据图表留在受控目录 `outputs/feat-010/a0/20260924-r2/`。机器审计通过；真实来源／标签抽查及图表人工验收待完成。
3. A1 按 `as_of=2026-08-01` 构建不含标签／折号的 61 天输入合同，检查事件窗、画像映射、车辆日覆盖和特征来源。运行产物与真实统计均保存在受控 `outputs/feat-010/a1/`。
4. 首次 A1 运行单独保留为中断尝试；回溯显示轨迹与 IMU 原始分片扫描已完成，进程中断于逐车路线重复度计算，完整 F3 文件尚未写出。该尝试的原因与状态记录在 `outputs/feat-010/a1/20260924-r1/failure.json`，未覆盖重用。
5. 为新运行优化轨迹和 IMU 分块按车辆的分组方式，路线重复度按自然日复用 haversine BallTree，并增加分片／车辆阶段进度。路线距离定义不变；`test_route_repeat_cached_balltrees_match_previous_two_way_reference` 以独立旧算法复算，新旧距离逐值一致。F3 相关合成测试通过。
6. A1 新运行 `20260924-r2` 已完成。F3 的 500 车特征、窗口里程／时长和 IMU 观测 sidecar 已生成；候选表、窗口合同、分布审计、两张真实数据图、受控验收页与 manifest 已写入同一 run。首轮在绘图 API 参数处报错，错误留在受控目录；修正为当前 Matplotlib 参数后，从已完成且逐车绑定核验过的 F3 输出续跑，没有重扫原始分片。
7. 新增独立 `check_feat010_a1.py`，复核无标签／折号、窗口、逐车键、F3 sidecar、字段合同、合成边界、产物哈希与原始分片清单指纹。真实运行检查通过；其真实来源和语义待查项仍需人工解释，A1 机器通过不代表字段全部获准入模。

## 验证与边界

- `pytest -q feature_engineering/tests`：83 项通过；包含路线重复度算法等价检查，以及 A1 checker 的正向、标签／折号泄漏和哈希篡改合成检查。
- `python3 -m unittest discover -s tests -v`：30 项通过。
- `python3 tools/check_governance.py`：working-tree 检查通过；`py_compile` 与 A1 独立 checker 对真实 run 的窗口、键、sidecar、合成检查和产物哈希验证通过。
- 61 天候选只用于输入语义和可观测性审计，不连接 20 天特征窗／40 天代理标签计算 AUC。
- 本轮未启动 B0/B1 模型训练或真实效果判定。EVAL-001、EVAL-003 和 FEAT-009 真实人工复核仍未完成。
- 所有真实数据、车辆级结果、图表、统计和数据指纹只存忽略目录 `outputs/feat-010/`；公开仓库只记录流程与结论边界。
- `EVAL-002.json`、`FEAT-008.json`、`MODEL-005.json`、`requirements.txt` 和既有 `run.py` 是工作区中其他改动，不纳入本任务。

## 接手者下一步

由叶安复核 A0/A1 真实图表与字段合同：重点确认 A0 标签／折分与来源抽查、A1 61 天窗口、车辆日覆盖边界、需字段／语义／来源复核的字段及排除族。复核记录落在受控验收页后再更新任务状态。B0/B1 在前置责任人真实复核完成前保持 blocked，不从 A1 分布审计推断模型增益。
