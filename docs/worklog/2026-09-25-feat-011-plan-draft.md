# 2026-09-25 FEAT-011 小规模建模适配方案代拟

- 任务／分支／基准提交：[FEAT-011](../tasks/FEAT-011.json)；本机 `main` 草案；`db937b86c8a4ce8405d218ca9d8a834a943fc0d0`。
- 人类责任人：叶安。执行者与 AI 协助：Codex 仅代拟文件。
- 复核状态与真实复核人：待叶安复核；未登记接受决策，未执行真实实验。

## 做了什么

依据 FEAT-009 v2 输入、已登记模型选择与 FEAT-010 未完成的来源／观测量复核，代拟一轮固定 2×2 特征视角／EBM 平滑对照与一次事先固定的预测融合。方案逐步给出输入、版本、归因比较、机器坏例、图表样张、判优门、计算预算和停止／回退规则。新建方案及 proposed 任务记录；没有修改 FEAT-009、FEAT-010、公共协议或王健祺线产物。

本机已有的任务复核记录、经验报告、依赖文件和 `run.py` 等未提交改动均未纳入本任务；本机 `main` 落后 `origin/main`，没有合并或重置。

## 验证

`python3 tools/check_governance.py` 通过（working-tree）；`python3 -m unittest discover -s tests -v` 通过（52 项）。本机没有 `python` 别名，首次按模板运行该命令得到 `command not found`，随后使用 `python3` 完成测试并在方案中使用相同解释器。没有运行模型、计算新指标或生成真实 OOF。草案未获人工确认，不提交／推送为正式路线。

## 决策与限制

无新增 ADR；FEAT-011 处于 `proposed`。FEAT-009 主跑、FEAT-010 观测量诊断和 MODEL-008 冲刺保持各自历史身份。本方案的 E−A 是唯一新主比较；所有结果若将来运行，仍属于同一开发样本反复使用后的代理证据。真实效果运行受 EVAL-001／EVAL-003／FEAT-009 责任人复核门约束。

## 接手者下一步

由叶安检查 [方案](../plans/feat-011-small-attribution-model-adaptation-plan.md) 的特征视角、四臂及固定融合是否符合目标。确认后按项目纪律登记接受版本、处理索引和远端同步；执行者再从 S0 的受控输入／列集审计开始。未确认时保留草案身份。
