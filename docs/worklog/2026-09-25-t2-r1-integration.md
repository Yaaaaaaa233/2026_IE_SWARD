# T2R-006：R1 新提交后的跨线对接

日期：2026-09-25
负责人：周航正
执行者：Codex
状态：review

## 发现的新进展

娄澜已在 `main` 上传：

- T2R-004：R1 扣分引擎无数据部分，包括事件并段、暴露归一、折内分档、EB 平滑、PAVA、规则压缩、组内边际递减和 R2/R4 输出接口；
- T2R-005：R3 融合实验骨架，包括 Q0/Q1 融合、`alpha_q0` 网格、同一 MOE 评价和分歧车辆清单。

仓库全量合成测试证明 R1 → R4 和 R1 → R2 主接口已经连通。T2R-004 交接同时提出透明弱基线与 R1 规则分均叫 Q0 的命名冲突。

## 本次完成

1. 把周航正 R4 中的“等存在透明弱基线”统一更名为 **B0**；`Q0` 唯一保留给 T2R-004 的 R1 学习规则分，Q1 仍为任务一概率转换分。
2. 将评价合同升为 `T2-R4-v1.1`，同步代码、文档、任务记录和测试名称。
3. 新增 `r1_publication.py`，在不修改 R1 引擎的前提下完成发布保护：
   - 正常车辆发布 R1 台账精确复算分；
   - `insufficient_evidence` 车辆发布 50 分保守先验；
   - 原始台账分保留为 `raw_rule_score`；
   - 输出 `observation_status`、`publication_policy` 和 `rule_version`。
4. 新增车辆集合、标签、折号、先验值和台账复算检查。
5. 新增合成端到端案例：R1 原始低证据 100 分经适配后为 50 分，能够通过 R4 硬检查、五分位方向门和逐折检查。

## 边界

没有修改娄澜负责的 `task2/deduction/`、`task2/fusion/` 或其测试。没有运行真实数据，也没有产生真实 R1、融合或任务一效果结论。50 分仍是 R4 当前候选先验，组长或训练折选择改变时需要升版合同。

## 下一步

R1 真实数据阶段到位后，先通过 `build_r1_publication_rows` 生成带低暴露保护的 Q0 发布行，再进入 R4 评价、R2 动态修正和 R3 融合。任务一 Q1 应引用当前经团队确认的同折 OOF 版本；MODEL-008 的新参考分已进入仓库，但是否作为正式 Q1 仍需版本与指纹复核。
