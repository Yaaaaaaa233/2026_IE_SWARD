# 2026-09-18 数据契约草案与合成接口模板

- 任务／分支／基准提交：DATACON-001｜`work/DATACON-001-data-contract`｜`79e3fad6496607d2eb90520a0a96297e40203f56`
- 人类责任人：待确认（数据线成员）
- 执行者与 AI 协助：ZCode agent 执行；数据线成员提供本地分析结论与会议口径
- 复核状态与真实复核人：未复核

## 做了什么

- 新增 [数据契约草案 v0.1](../interfaces/data_contract.md)：五张表与两张配套表的字段、类型、主键、缺失语义、时间与窗口语义、版本字段、守恒断言，以及规则层清洗方法。不含任何真实车辆标识、真实统计或数据指纹。
- 新增 [接口模板](../interfaces/templates.md)：七张表的合成示例，明确标注为合成数据；可直接用于合成数据测试。
- 在 [契约入口](../interfaces/README.md) 新增一行索引，未改既有约定。
- 登记 [DATACON-001](../tasks/DATACON-001.json)，状态 `review`，等待复核。

未纳入的已有改动：无；本分支仅含上述文件。

## 验证

- `python tools/check_governance.py`（working-tree）与 `--scope index`（暂存后）通过。
- `python -m unittest discover -s tests -v` 通过。
- 未运行任何模型或数据实验；本任务不涉及。

## 决策与限制

- 无 ADR。契约状态为草案：字段命名与窗口口径在冻结前可能调整，冻结须按决策流程补充 ADR。
- 依据 [数据边界](../DATA_POLICY.md)：含真实统计的分析报告、真实值接口模板（CSV）、原始数据与折外预测保存在受控本地目录（数据线成员的 `任务一数据分析与分工/` 目录），不进入本仓库；本仓库内示例全部为合成数据。
- 会议口径（预测窗口 40 天等）与既有评估口径存在待拍板差异，契约将窗口字段参数化处理，未替团队做选择。
- 任务前缀 `DATACON` 为数据线新起编号，需协调者确认是否沿用。

## 接手者下一步

1. 数据线成员复核契约字段命名与语义，确认后按决策流程提 ADR 冻结或列修改意见。
2. 协调者确认 `DATACON` 前缀与状态页 M0 行的对应关系。
3. 基线任务确认可依据本契约设计读取原型；真实数据测试在授权本机执行，产物不入库。
