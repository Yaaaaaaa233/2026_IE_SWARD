# FEAT-008 r5 执行记录（终轮；完成时更新）

- 任务：[FEAT-008](../tasks/FEAT-008.json)｜状态：active（叶安 2026-09-23 裁定"r5 修复版一次"）
- 执行方案：[feat-008-r5-execution](../plans/feat-008-r5-execution.md)（§2 预登记口径先行落盘）
- 分支：`main`（直推）；基准提交：`9d01b3a`
- 人类责任人：叶安；执行：DSH 主会话＋deepseek-v4.1-flash 子智能体

## 背景（一句话）

r4 归因揭示"新信息主力 0 列入模"（40 列预算被夜间分场景挤占）＋L2 查出 P1–P5 真实数据问题——r5 修复后做新信息路线的第一次真实检验，也是特征侧最后一轮判定。

## 计划（引用执行方案，此处只记进度）

1. P1–P4 代码修复＋族配额 select_new_columns＋合成测试——**完成**（提交 97b1631：trajectory/pipeline/interface/core 修复＋测试新增 13 项共 79/79 全绿；P3 本地配置核实指向基座列名 highway_ratio；P5 代码对齐预登记字面公式 30.44，公式字面落 plan §2 与 core 文档串）
2. 重跑 run-all → interface——**完成**（F3R5_v1＝500×104：lean 基座 71＋新增 33；**13 族全部入模**——轨迹族 14 列（spell 3/speed 4/spatial 4/rhythm 3）＋同群 3 列，r4 "0 列入模"构成缺陷修复在真实数据确认；manifest 族计数登记）
3. 统一评估器 ×3 种子＋配对判定（一次）＋5 块归因——**完成**（见下）
4. L1/L2 轻量验收与登记收口——**完成**（L1 契约断言随组装通过＋版本三件套齐；L2 轻量三项全过，材料 outputs/acceptance/FEAT-008.md；未过门不补全 L3，与 §4 口径一致）

### 执行中随记（随执行登记）

- **提交哈希映射（rebase）**：修复提交原 97b1631，因队友并行推送 rebase 后为 1dae11d；feature_f3r5_run manifest 的 code_commit 记录的是扫描时点的 97b1631（rebase 前哈希，内容与 1dae11d 逐字节一致）——版本三件套追溯以此为准。

- 驻车步判定采**到达点语义**（到达点速度=0 或到达位移 <50m）：驶离步出发点速度=0 不误判驻车（合成测试暴露后修正）。
- P1 跳变步同时从里程与运行时长中剔除（切段语义下该步不属任何连续段，随执行登记）。
- P4 顺带对齐日界：日级特征（日里程 CV/断档天数/路线重复日对）按本地日历日（+8h）聚合，与 core burst 口径一致；断档天数因窗口边界跨入部分本地日普遍 +1（常数平移，无害）。
- 族配额的族间顺序＝预登记列举序轮转、score 恒优先＝整族最先入选；per_100h 双轨列不占配额（收尾通则必剔）。
- 归因消融块映射：b5 事件结构＋技术修正含 other 族（f3_fatigue_night_conc，随执行登记）；u0 综合分同 r4 作补充块（r5 起为正式块定义，仍标非预登记）。

## 结果（2026-09-23 判定；数字明细入受控台账 §8）

- **配对判定（一次，seed42 主判定）**：F3R5_v1 对冻结基线 RF@F0_v1 ΔAUC **+0.0093，95% CI [−0.0072, +0.0268]，未过门**（CI 跨零）；seed7 +0.0068 [−0.0103, +0.0242]、seed2026 +0.0105 [−0.0073, +0.0293] 三种子同向；容忍度全过（Recall@100/Brier）。
- **L2 轻量三项全过**：P1 修复车 95202153 窗内里程由伪迹量级回到货车物理范围（过滤剔除 115 个跳变步、重算计数与管线产出精确一致，p999 隐含速度回到阈值内）；P2 r4 病灶两车（最长连续驾驶 480h 退化、gt4h 恒 1）r5 回到个位数小时量级、gt4h 份额脱离恒 1；P3 同群群键能源×高速占比折内中位二分 hi/lo 双支全非退化（na=0）。
- **归因消融（5 块＋补充，seed42，仅归因）**：①剪枝基座去块后下降最多（唯一最大正贡献块）；**④轨迹族首次真实入模并读出正向点估计（区间含 0）**——r4 "未检验"状态就此收口；补充综合分正向；②历史时间结构、⑤事件结构＋技术修正两块去块后反而改善（⑤区间不含 0）——这两族在 r5 构成下为净负贡献。
- **判读**：新信息路线（轨迹/同群）真正入模后效应仍为 +0.01 量级、与 r4（+0.0098）几乎不变——**"未检验"假说被回答：构成修复改变的是归因可见性，不是总效应**。六轮形态定型（r1~r5 特征侧 +0.009~+0.012 全部 CI 跨零），结构性瓶颈（真实效应 +0.01 vs n=500 功效极限 ~0.015）终局确认。
- **收口**：按预登记 §1/§5，**r5 为特征侧最后一轮判定，未过门→特征侧迭代收口**；基线不棘轮（仍 RF@F0_v1）。进入阶段 3（EVAL-003）须**人工宣告**启动（纪律：代理不自行推进）；F3R5_v1 特征底座与事件级产物移交任务二。
- 产物：`feature_f3r5_run/`（f3_new_features 500×127＋model_interface model_input 500×104＋manifest/剔除清单）；`outputs/feat-008/round-1/`（eval_f3r5_s{42,7,2026}/analyze_r5.py/paired_results.json/6 变体归因/l2_r5_checks.py+l2_r5_report.json）。
