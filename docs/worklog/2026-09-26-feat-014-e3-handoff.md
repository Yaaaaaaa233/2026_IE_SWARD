# 2026-09-26 FEAT-014 E3 收口与交接

- 任务：[FEAT-014](../tasks/FEAT-014.json)，`review`；执行分支 `work/feat-014-execution`；基准工作提交为本次 E3 收口前的分支提交。
- 人类责任人：叶安。执行者：Codex。方案授权：叶安 2026-09-26 明确要求按方案连续执行，不在候选版本间等待确认。
- 复核状态：E0–E3 技术和机器审计通过；真实图表及候选结论待叶安实际复核，不登记 `accepted`。
- 受控运行：run ID `20260926-e0-r2`，位于忽略的 `outputs/feat-014/`。OOF、指标、图表、环境补充记录和输入指纹均留在受控运行目录，没有加入 Git。

## 做了什么

完成 FEAT-014 方案内 E0–E3。E0 固定了 FEAT-009 v2 标签、五折、开发窗、RF 与 F3 两个参照，并复核可复用的历史 OOF。B001–B003 完成特征／模型候选、误差诊断和固定权重融合；B004–B010 按任何新增种子拟合开始前锁定的注册表，完成单模及融合路线的多种子比较、均值 OOF 和 seed 42 同配置复跑。

总计 25 个新评估版本，未超过 30 版上限。E3 登记 11 版而不是预算建议中的 6 版稳定性配额，原因是每个新增种子的三成员固定融合需分别生成成员模型 OOF 和融合 OOF，再增加均值 OOF 与同种子复跑；分配已在拟合前登记，仍保留 5 版预算，不为用满预算继续搜索。

当前候选按角色保留为：最高探索点 `B008/V023`、较小种子跨度路线的均值 `B009/V024`、低成本备选 `B004/V017`。三者均满足辅助 Recall@100／Brier 门，但相对固定 F3 的 AUC 区间下界未高于零，且未达到既有的 +0.01 实际意义下界。因此结论是“本轮无确认提升”，不是“模型无效”；代理 OOF 上的点估计和多轮择优带有选择偏差。

生成 D0–D3 全版本曲线、种子稳定性图、固定锚点门槛图和消融对照图。新增 `finalize.py` 生成受控报告／台账／图表，新增 `audit_e3.py` 独立核对注册计划 SHA-256、反馈证据指纹、E1 批次状态、版本总数、种子指标及区间、固定 logit 组合、候选门槛和复跑差异；独立 E3 审计通过。

## 验证

- B001–B010 的每批 E1 独立审计通过；B001–B003 的 E2 诊断／审计也通过。
- E3 独立审计通过：25 版台账完整，候选和路线选择可复算，固定融合 OOF 与注册成员重算相符，seed 42 同配置复跑 OOF 完全一致。
- 完整单测 95 项通过；`python3 tools/check_governance.py` 通过（working-tree）；`git diff --check`、任务 JSON 解析及 E3 脚本编译检查通过。
- 复现入口：受控环境见 `input_lock.json` 与 `environment_reconstruction.json`；E3 汇总命令为 `python3 feature_engineering/experiments/feat014/finalize.py --run-dir outputs/feat-014/<run-id>`，独立检查为 `python3 feature_engineering/experiments/feat014/audit_e3.py --run-dir outputs/feat-014/<run-id> --write`。

## 决策与限制

未新增 ADR；本轮遵守 FEAT-014 既有评定口径。25 个版本复用同一开发代理标签和折分，bootstrap 区间不能修正反复查看同一代理标签导致的自适应择优，种子跨度也不是独立测试区间。B002 初次执行时发现 shell Python 缺少 `interpret`，随后按锁定核心版本重建环境并补齐已记录的 PyYAML 依赖；五个已注册版本最终均有完整 OOF 和通过审计的结果，初始失败尝试保留在受控运行记录中。

本轮没有进行官方最终窗口重训或推理。候选经人工复核后，才按 2026-06-01 至 2026-07-31 全 61 天训练、预测 2026-08-01 至 2026-09-10；逐项复核数据可用性、特征截止时点和推理输出。复核／复现或交付校验失败时，回退至 FEAT-009 v2 固定 F3 EBM-A 配置与产物。此结果是叶安线开发期探索，不比较或约束王健祺、吴钊同算法线。

## 接手者下一步

1. 叶安查看受控 `report.md` 和 D0–D3 实图，记录图表是否通过及对候选结论的复核；随后由责任人更新任务状态。
2. 如决定使用候选，另开明确登记的 61 天全窗构建和推理任务，保留本次运行目录作为只读回退证据；不将代理 OOF 分数直接写成正式成绩。
3. 对外同步只提交本文件、任务状态和实现代码；不提交受控输出目录。
