# 2026-09-20 评价指标与基线比较方案

- 任务：[EVAL-001](../tasks/EVAL-001.json)
- 分支：`work/EVAL-001-evaluation-metrics`
- 基准提交：`454f20b45b3c7412fd6024b1e2e4f1b19a4c439d`
- 人类责任人：待确认
- 执行者与 AI 协助：Codex，按用户直接请求整理并上传评价指标
- 复核状态：实现者自查完成，任务置为 `review`；没有代签人类复核

## 做了什么

新增[指标方案 v0.1](../interfaces/evaluation_metrics.md)，将 MOE 与 MOP 对应到任务一风险识别、有限管理名额、概率质量及任务二评分可用性。集中说明 AUC、AP、Top-K、Brier、Log Loss 的方向与公式、同分和异常输入处理、拟合与校准边界、配对比较、判优及工程检查。

对照仓库已有官方赛题说明核实 AUC 排名和任务二四项权重，保留 30/40 天未决事项。新增 Proposed 的 [ADR-0002](../decisions/ADR-0002-evaluation-metrics.md)，并在公共契约、验收规范、决策及任务入口增加链接。写入范围以任务 JSON 为准；未修改其他工作线的数据契约、模型代码或全局状态页。

公开内容只有方法、规则和明确标注的合成算例，未提交真实样本规模、正类统计、车辆明细、折外预测、模型、数据派生图表或本机配置。

## 验证

- `python tools/check_governance.py`：通过，检查结构、链接、任务元数据和已知公开边界。
- `python -m unittest discover -s tests -v`：17 项现有单元测试通过，使用合成仓库，不依赖比赛数据。
- `git diff --check`：通过。
- `python tools/check_governance.py --scope index`：通过；本交接补记后再次对最终暂存版本检查，通过后才提交。
- 人工检查：公式、正负方向、合成算例、官方权重、跨车辆与时间外推区别、未冻结状态及公开内容边界已核对；无新增评估器可供算法测试。

| 维度 | 状态与范围 |
| --- | --- |
| 实现与契约 | 文档及导航完成；评估契约仍 Proposed，未实现指标计算程序 |
| 实验效果 | `not_run`，未训练模型，也未读取比赛原始数据 |
| 验收与证据 | 文档治理与既有测试通过；没有真实模型成绩或独立人类复核 |
| 环境与依赖 | 文档检查可运行；没有据此验证机器学习运行环境 |

## 决策与限制

ADR-0002 仅为提议。指标文档上传和 PR 本身不代表正式评估协议已接受，也不解决原状态页的官方口径、标签、数据映射或模型路线问题。配对 bootstrap 只提供基于既有折外预测的近似不确定性，不能证明实际事故减少。

## 接手者下一步

1. 由真实责任人与复核人审阅指标优先级、运营容量、误差容限及判优门槛。
2. 依据官方澄清与数据审计冻结时间和标签口径，再完成 ADR 接受记录；正式实现需另建任务并引用版本。
3. 后续评估器用合成数据检验等分、空集、单一类别、非法概率、重复键和时间泄漏等边界，真实运行结果只保存在受控目录。

本次停止条件为文档与导航完整、必需检查通过、任务分支与 PR 可评审；不扩展为模型训练、公开实验结果或自动合并。
