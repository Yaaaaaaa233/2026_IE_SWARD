# FEAT-007 r4 登记与执行方案（开工版，完成时更新）

- 任务：[FEAT-007](../tasks/FEAT-007.json)｜状态：active（叶安 2026-09-22 确认开工）
- 执行方案：[feat-007-r4-execution](../plans/feat-007-r4-execution.md)（含 §3.0 预登记定义与 §5 验收细则）
- 分支：`main`（直推）；基准提交：`6502cb3`
- 人类责任人：叶安（仓库建立与纪律管理）；执行：DSH 主会话＋deepseek-v4.1flash 子智能体
- 复核状态：方案已经叶安确认（2026-09-22）；AI 产出代码仍标**待复核**，未经人工核查不标记完成（AI 产出人工核查纪律）

## 背景（一句话）

r1／r2 全族正向未过门；FEAT-006 r3 实证全量叠加共线稀释（+0.0094 未过门）、剪枝改善 CI 但主种子未过门——**已有信息重组到边际，缺的是新信息**。r4 转向轨迹／同群／画像偏差／夜间退化四条新矿脉，并入 lean 瘦基座一次判定。

## 编号说明

原拟号 FEAT-006 与并行工作线撞号（该号已用于 y 盲剪枝 bundle 并执行完毕），按协调规则让号 FEAT-007（迭代期 r4）；两线互补：FEAT-006 答"已有信息怎么摆"，FEAT-007 答"新信息怎么来"。

## 计划（引用执行方案，此处只记进度）

1. stage 0 三项技术修正＋Tier 1／3 特征与合成测试——待执行
2. 轨迹一遍流式扫描＋Tier 2 特征——待执行
3. F3_v1（lean 基座＋新增族）组装＋统一评估器配对判定（一次）＋归因消融——待执行
4. L1／L2／L3 验收与版本三件套登记——待执行

## 进度（2026-09-22 深夜，编码轮）

1. ✅ **代码＋合成测试入库**：stage 0 工具（排序防御/率分母去重/近常数方差排除）＋Tier 1（历史时间结构/同群相对化/画像偏差/夜间退化度）＋Tier 2（切段/速度形态/空间热点/日节律）＋Tier 3（连环共现/EMS-GPS）＋interface/pipeline/cli＋配置模板；合成测试 51/51 全绿（新增 38 项）——deepseek-v4.1-flash 子智能体执行、主会话复核 L1。
2. ✅ 轨迹一遍扫描（33GB＋IMU 轻扫 83GB）与 F3_v1 组装完成：三次运行收敛（①事件 NaN 坐标崩溃→健壮性全面硬化 7c88444；②同群源列名配置错误→按真实表头修正；③事件表契约窗口化——标签窗事件 2,116,062 条不入聚合、窗内 977,234 条，聚合侧窗口掩码双保险）。产物 f3_new_features.csv（500×126）→model_input.csv（108 特征＝基座保留 71＋新增 37≤40 上限；收尾通则剔除清单 6 组含 F0 恒 0 死列与高共线列），manifest 带代码提交号，契约断言全过。（历史记录）原 2. 轨迹一遍扫描与 F3_v1 真实组装——运行中（数据定位完成：轨迹 33GB/IMU 83GB 无表头 TSV；规范化输入与本地配置已就绪；管线补丁两件入库 01209a6：无表头分片支持＋特征窗预过滤＋IMU 双速度源轻扫；stage 0 探针结论：'运行时长'非累计→切段 gap_180min 口径，事件 24 类 token 映射登记（41001/41002/41029 归 fatigue_* 族））。新增待 L2 复核口径：IMU |EMS−GPS| 统计无最低样本门槛（§3.0 未预登记）、p95 固定直方图箱中位近似。
3. 🔄 配对判定（一次）＋归因消融——统一评估器 ×3 种子运行中（seed42 主判定，7/2026 旁证）。
4. ⏳ L1/L2/L3 验收与版本三件套——待 3。

**代码待人工核查（L2 语义复核重点）**：① night_degradation 门槛 500/300/100 的作用域映射（窗级合计 500／合并版逐侧 300／分场景版逐侧 100）；② 日级统计 min_days=5（§3.0 未点名，随 docstring 登记）；③ 组装层 docstring 登记的实现口径（事件时速=窗内事件车速均值、夜间暴露=深夜点占比、疲劳报警夜间集中度=疲劳类事件深夜占比、基座缺列剔除 reason=missing_in_base_input）。

## 结果（2026-09-23 晨收口）

- **配对判定（预登记唯一一次；seed42 主判定，判据 ADR-0004 不放松）**：F3_v1（lean 基座保留 71＋新增 37）对 RF@F0_v1 **ΔAUC +0.0098，95% CI [−0.0064, +0.0275]——未过门（CI 跨零）**；旁证种子同向为正（seed7 +0.0055、seed2026 +0.0099）；容忍度全过（Recall@100/Brier 未恶化）。**负结果登记，基线不棘轮（仍 RF@F0_v1）**。数字全量见受控台账 §8 与 outputs/feat-007/round-1/paired_results.json。
- **四轮总形态定型**：r1/r2/r3/r4 的 bundle+剪枝+新信息路线全部点估计 +0.01 量级、CI 跨零——特征侧单轮增量已系统性低于 n=500 配对判定可分辨下限（与瓶颈分析功效判断一致）。新信息矿脉（轨迹/同群/画像偏差/夜间退化）方向为正但未改变格局。
- **产物移交**：F3_v1 model_input（500×108）与 f3_new_features（500×126，含疲劳结构/夜间风险链/热点复发等可解释族）按任务二可消费 schema 落 feature_f3_v1_run/（manifest＋剔除清单＋代码提交号指纹）；事件级场景 token 映射登记 outputs/feat-007/round-1/scenario_token_map.json。
- 归因消融（预登记 5 块）与 L2 抽样报告：补跑登记中，供后续轮次与任务二叙事参考（不作采纳依据）。
- **r4 收口方式待人工裁定**（§4 预登记）：与模型线汇合放大对齐救回效应／人工宣告进阶段 3 终评／重心转任务二——agent 不自行推进。
