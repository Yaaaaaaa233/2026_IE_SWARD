# 配置与本地环境

复制 [local.example.toml](local.example.toml) 为 `local.toml`，只在本机填写路径。该模板不含秘密、真实设备标识或固定机器目录。

目前尚无算法运行程序读取它；后续实现需显式加载并校验目录，不能把模板存在解释为数据管线已完成。

可共享的实验配置后续按契约建立，记录窗口、折分版本、特征与模型参数；不把本地数据位置混入共享配置。环境依赖与锁文件由首个实现任务确定。
