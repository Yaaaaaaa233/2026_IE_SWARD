# 冲刺冠军版 sprint-g6v5-H2（任务二参考数据，2026-09-25 第二轮后更新）

任务：[MODEL-008](../../docs/tasks/MODEL-008.json)（组长 2026-09-25 指示将当前最优版上传，供任务二作参考数据）
身份：任务一冲刺当前最优（协议 v2 代理回测 OOF 融合概率；非官方未来窗成绩，非 ADR-0004 正式采纳版——采纳需组长复核＋与叶安线逐车对账）

## 版本构成

四成员预测级加权融合（logit 尺度，权重 0.53 / 0.19 / 0.08 / 0.20）：

1. **g1 事件序列融合臂（0.65）**：EBM-A + EBM-C + RF 三模 logit 平均，特征 = 事件表序列 53 列（语义率 per_1000km、险情时间结构、多窗口、类型转移/间隔分布/昼夜/连环/日节律）；
2. **g2 面板 EBM-C（0.245）**：特征 = vehicle_day 纵向面板 24 列（趋势/波动/断档/节律/覆盖/活跃）；
3. **g3 IMU EBM-C（0.08）**：特征 = IMU 方向无关模量 18 列（冲击/水平/旋转分位与超阈率、双速度源差异）。
4. **g6 语义聚类 EBM-C（0.20）**：特征 = 语义率双轨 + K4/K5 语义聚类簇内相对 z + 能源 cohort z + 险情结构 + 轨迹精选 51 列（组长设计的语义聚类+暴露归一+IMU/轨迹增强路线）。

协议：label_v2_record_count_20260923 / split_v2_record_strat5_seed42（54 正类）；特征窗 [2026-06-01, 2026-06-21)。
锚点对照（受控数字，见 worklog）：vs RF@F0v2 +0.0489，95% CI [+0.0199, +0.0812]，区间下界>0（三成员前代的种子稳健性：均值 0.8525、波动 0.0048）。

## 任务二消费方式

- 读 `model_score.csv`：`gpsno, prob, rank_pct, calib_flag, model_version`（500 行，无标签无原始数据派生特征）。
- Q1 概率转换分按路线图：S = 100 × (1 − prob)；prob 为折外融合概率（同批同源，可直接排序）。
- 版本切换时按指纹登记（本表 prob 为 OOF 融合值；正式预测文件待采纳身份确定后另行生成）。

## 复现入口（代码同步入库）

- `reproduce_champion.py`：从受控特征表与 OOF 重建融合与 model_score（数据路径走本地受控配置）。
- 特征构建/扫描与评估栈源码：`src/`（从受控 `任务1工作冲刺/_common` 与各组构建脚本同步的公开副本，真实数据路径不入库）。

## 边界

- 本目录按组长明确授权入库；`model_score.csv` 含车辆概率派生数值（数据政策默认不入库项，授权记录见本 README 与 MODEL-008 worklog）。
- 该版本尚未通过与叶安线 FEAT-009 的逐车对账；两线合并终评前不作为团队最终提交版本。
